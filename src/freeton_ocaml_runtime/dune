; generated by drom from package skeleton 'library'

(library
  (name freeton_ocaml_runtime)
  (public_name freeton_ocaml_runtime)
  (wrapped true)
  (libraries  )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package freeton_ocaml_runtime))


